#include "Base.xcconfig"
#include? "Local.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER)
DEVELOPMENT_TEAM = $(APP_DEVELOPMENT_TEAM)
INFOPLIST_KEY_JapanVoiceWorkerBaseURL = $(JAPAN_VOICE_WORKER_BASE_URL)
INFOPLIST_KEY_JapanVoiceAppSharedSecret = $(JAPAN_VOICE_APP_SHARED_SECRET)
